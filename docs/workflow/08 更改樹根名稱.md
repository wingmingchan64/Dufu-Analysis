# 08 更改樹根名稱

Status: Draft

---

## 槪述

本文介紹如何更改樹根名稱。

---

## 步驟

- 加一個新的根節點
- 把舊的根節點値賦値給新的根節點
- 去掉舊的根節點

<pre>
$old_name = '0003';
$new_name = '0099';
$基準正文樹[ $new_name ] = $基準正文樹[ $old_name ];
unset( $基準正文樹[ $old_name ] );
</pre>